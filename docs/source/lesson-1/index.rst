Lesson-1
==========

We can use below commands to create vue vuetify project:

.. code-block:: bash
    :linenos:

    $ vue create todo-lesson01
    $ cd todo-ninja
    $ npm run serve

    // add vuetify
    $ vue add vuetify